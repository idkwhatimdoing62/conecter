# Conecter — PRFAQ / 产品需求文档

## 新闻稿（Press Release）

今天，Conecter 发布一种更直接的文件传输方式：同一局域网内的设备可以把文件拖入浏览器，立即生成带密钥的短链接；接收者无需注册账号、无需等待云端同步，只需打开链接并输入密钥即可下载。Conecter 解决了“临时传一个大文件”时最常见的摩擦：登录、压缩、上传到公共网盘、复制复杂权限。文件默认仅在发送设备与接收设备之间流转，发送者能看到实时进度、连接设备和有效期，并可随时撤销分享。

产品面向家庭、办公室、教室和创作团队中的临时协作。它支持桌面与移动浏览器，链接可通过二维码或复制分享。传输过程采用端到端加密；服务端只负责发现设备、协商连接和短期元数据，不持久化文件内容。即使接收者不在同一网络，也可通过安全中继完成下载，但界面会明确提示网络路径与隐私级别。

Conecter 的成功标准不是“上传了多少”，而是从拖入文件到另一台设备开始下载的时间、成功率和无需帮助即可完成的比例。未来将加入批量文件夹、断点续传、团队空间和原生客户端。

## FAQ

**谁会使用？** 发送者是需要快速交付文件的人；接收者是拿到链接与密钥的人；管理员仅在团队模式中管理策略与审计。

**核心路径？** 发送者打开首页 → 拖拽/选择文件 → 设置有效期（默认 24 小时）→ 生成链接与 6 位密钥 → 复制或扫码；接收者打开链接 → 输入密钥 → 预览文件清单 → 下载。

**必须支持什么？** 多文件批量选择、上传进度、暂停/取消、链接撤销、密钥错误提示、过期态、断网重连、移动端布局和键盘操作。

**不做什么？** MVP 不做账号体系、长期云盘、在线编辑和公开搜索。

**边界与异常？** 单文件默认 5 GB；磁盘不足、浏览器不支持 WebRTC、局域网隔离、密钥连续错误、分享者关闭页面都需要给出可理解的恢复建议。

**数据结构？** Share：`id, secretHash, createdAt, expiresAt, status, ownerDevice, networkPath`；Asset：`id, shareId, name, mime, size, checksum, transferState`；Peer：`id, label, ipHint, lastSeen`；Event：`type, timestamp, assetId, peerId`。

## PRD 摘要

### 目标用户

个人临时传输、局域网小团队、课堂/会议现场、设计与开发协作。

### MoSCoW

| 优先级 | 范围 |
|---|---|
| Must | 发送/接收双入口、拖拽上传、链接+密钥、进度与状态、撤销/过期、响应式与无障碍 |
| Should | 二维码、批量文件、断点续传、局域网设备列表、下载历史 |
| Could | 文件夹、品牌化分享页、团队策略、分享备注 |
| Won’t（MVP） | 账号、长期存储、在线编辑、公开文件发现 |

### 信息架构

`首页 → 发送 / 接收 → 分享详情 / 下载详情 → 完成 / 异常帮助`；全局入口包含网络状态、最近分享和设置。

### 任务流

发送：选择文件 → 校验大小/类型 → 生成分享 → 等待连接 → 传输 → 完成/撤销。

接收：打开链接 → 校验密钥 → 查看清单 → 选择下载位置 → 下载 → 校验完整性。

## 设计转译

- **Josef Müller-Brockmann**：借鉴网格、留白和信息层级；转译为严格 12 列栅格、等宽数字进度与左对齐状态信息，不复制其海报构图。
- **Dieter Rams**：借鉴“尽可能少但尽可能好”的产品克制；转译为单一主操作、清晰状态文案、低阴影卡片和可撤销的安全反馈，不复刻具体产品外观。

## 设计系统

- 色彩：`ink #18201C`、`paper #F6F7F4`、`surface #FFFFFF`、`signal #1F8F65`、`cobalt #356AE6`、`coral #D96B52`、`line #E0E5DF`。
- 字体：标题 Space Grotesk；正文 DM Sans；中英混排回退 `Inter, "Noto Sans SC", sans-serif`；数字使用等宽轴。
- 栅格：最大宽度 1000px，12 列，间距 24px；间距基准 4px，圆角 8/14px。
- 断点：<600 mobile，600–960 tablet，>960 desktop；点击目标最小 44×44px。

## 关键页面线框

1. 首页：顶部品牌/网络状态；左侧超大标题，右侧发送/接收卡片；下方最近分享列表。
2. 分享详情：文件清单与总进度为主视觉；右侧链接、密钥、二维码、撤销按钮。
3. 下载详情：分享者名称与有效期；文件卡片列表；主按钮“下载全部”；错误和过期状态占据同一内容区域。
